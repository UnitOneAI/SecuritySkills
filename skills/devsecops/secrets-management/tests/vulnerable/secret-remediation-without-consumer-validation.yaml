finding:
  id: SEC-001
  secret_type: github_pat
  location: build.log
  action_taken: removed_log_line
  revoked_at: null
  replacement_version: gh-pat-v2
  old_secret_disabled: false
  dependent_services_restarted: false
  consumers_validated:
    - api
  missing_consumers:
    - worker
    - cron
    - webhook-relay
  audit_log_review: not_started

# Vulnerable fixture:
# Removing the visible secret and creating a replacement is not enough. The old
# credential is not revoked or disabled, consumers are incomplete, and audit-log
# review has not been performed.

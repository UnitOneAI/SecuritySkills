name: deploy

on:
  push:
    branches:
      - main

jobs:
  production:
    uses: external-org/shared-workflows/.github/workflows/deploy.yml@main
    secrets: inherit
    with:
      environment: production

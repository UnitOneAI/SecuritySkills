name: deploy

on:
  pull_request:
  push:
    branches: [main]

jobs:
  deploy:
    uses: example-org/shared-workflows/.github/workflows/deploy.yml@main
    secrets: inherit
    with:
      environment: production

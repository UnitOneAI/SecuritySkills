apiVersion: skills.unitone.ai/v1
kind: SkillIndex
skills:
  # ... existing skills ...
  - name: support-bot-action-approval-review
    displayName: Support Bot Action Approval Review
    category: auth
    severity: medium
    description: Detect support bots that can trigger account or billing actions and ensure stronger approval and actor separation.
    # Additional fields as necessary (e.g., tags, relatedSkills)